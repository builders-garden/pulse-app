import {View} from 'react-native';
import {Profile} from '../../../api/profile/types';

interface CommentsSectionProps {
  profile: Profile;
}

function CommentsSection({profile}: CommentsSectionProps) {
  return <View></View>;
}

export default CommentsSection;
